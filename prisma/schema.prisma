generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            String    @id @default(cuid())
  email         String    @unique
  name          String?
  passwordHash  String
  country       String    @default("GB")
  currency      String    @default("GBP")
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  coverages     Coverage[]
  alerts        Alert[]
  transactions  Transaction[]
  assets        Asset[]
  claims        Claim[]
  familyMembers FamilyMember[]
  documents     Document[]
  gapDismissals CoverageGapDismissal[]
}

model FamilyMember {
  id        String   @id @default(cuid())
  name      String
  relation  String
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@index([userId])
}

model Coverage {
  id            String   @id @default(cuid())
  provider      String
  type          String
  category      String
  policyNo      String?
  status        String   @default("active")
  statusLabel   String   @default("Active")
  covered       String
  startDate     String
  endDate       String
  premium       Float    @default(0)
  deductible    Float?
  oopMax        Float?
  coverageLimit String?
  coInsurance   String?
  exclusions    String   @default("[]")
  claimPhone    String?
  claimUrl      String?
  summary       String?
  confidence    Float?

  userId        String
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  documentId    String?
  document      Document? @relation(fields: [documentId], references: [id])

  alerts        Alert[]
  gapDismissals CoverageGapDismissal[]
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([userId])
  @@index([category])
  @@index([status])
}

model CoverageGapDismissal {
  id            String   @id @default(cuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  coverageId    String
  coverage      Coverage @relation(fields: [coverageId], references: [id], onDelete: Cascade)
  gapKey        String
  dismissReason String
  dismissedAt   DateTime @default(now())

  @@unique([userId, coverageId, gapKey])
  @@index([userId, coverageId])
}

model Alert {
  id         String   @id @default(cuid())
  type       String
  severity   String
  title      String
  detail     String
  date       String
  read       Boolean  @default(false)

  coverageId String?
  coverage   Coverage? @relation(fields: [coverageId], references: [id], onDelete: SetNull)
  userId     String
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  createdAt  DateTime @default(now())

  @@index([userId])
  @@index([read])
}

model Transaction {
  id             String  @id @default(cuid())
  date           String
  merchant       String
  amount         Float
  card           String
  cardType       String
  category       String
  coverageStatus String
  benefits       String  @default("[]")
  missedOpp      String?

  userId         String
  user           User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt      DateTime @default(now())

  @@index([userId])
}

model Asset {
  id              String  @id @default(cuid())
  name            String
  category        String
  value           Float
  purchaseDate    String
  photos          Int     @default(0)
  lastPhotoUpdate String?
  coverages       String  @default("[]")
  riskLevel       String  @default("low")
  riskNote        String?

  userId          String
  user            User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([userId])
}

model Claim {
  id         String  @id @default(cuid())
  incident   String
  date       String
  provider   String
  category   String
  status     String  @default("not_started")
  step       Int     @default(0)
  totalSteps Int     @default(5)
  steps      String  @default("[]")
  nextAction String?
  deadline   String?

  userId     String
  user       User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([userId])
}

model Document {
  id           String   @id @default(cuid())
  filename     String
  mimeType     String
  size         Int
  storagePath  String
  parsedData   String?
  confidence   Float?

  userId       String
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  coverages    Coverage[]

  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([userId])
}
